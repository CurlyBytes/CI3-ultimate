{
	"description" : "The CodeIgniter Application with Composer",
	"scripts": {
		"check-tests": [
			"vendor/bin/phpunit --dump-xdebug-filter bin/xdebug-filter.php --prepend bin/xdebug-filter.php -vvv --testdox --colors=always",
			"vendor/bin/phpunit --prepend bin/xdebug-filter.php -vvv --testdox --colors=always"
		]
	  },
	  "scripts-descriptions": {
		"install-production": "Install the standard setup with code efficiently."
	  },
	"require": {
		"php": ">=5.3.2",
		"codeigniter/framework": "3.1.11"
	},
	"require-dev": {
		"mikey179/vfsstream": "1.1.*",
		"kenjis/ci-phpunit-test": "^0.17.3"
	}
}
